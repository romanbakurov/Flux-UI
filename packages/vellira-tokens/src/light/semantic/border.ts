import { colors } from '../../primitives/colors.js';

export const border = {
  default: colors.vellira[300],
  muted: colors.vellira[150],
  strong: colors.vellira[250],

  focus: colors.primary[700],
} as const;
